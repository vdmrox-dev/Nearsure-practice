import styled from "styled-components";
import {
  color,
  border,
  layout,
  space,
  flexbox,
  fontSize,
  fontWeight
} from "styled-system";

const Box = styled.div`
  ${color}
  ${layout}
  ${border}
  ${space}
  ${flexbox}
  ${fontSize}
  ${fontWeight}
`;

export default Box;
